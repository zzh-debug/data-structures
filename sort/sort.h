#ifndef SORT_H
#define SORT_H

void bubbleSort(int arr[],int n) {
    int tmp;
    for(int i=0; i<n; i++) {
        for(int j=0;j<n;j++) {
            if(arr[j] > arr[j+1]) {
                temp = a[j];
                a[j] = a[j+1];
                a[j+1] = temp;
            }
        }
   }
} 
